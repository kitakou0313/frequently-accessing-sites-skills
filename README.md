# frequently-accessing-sites-skills
情報収集用のサイトを周回、情報を集めるSkill

## ToDo
- Chrome MCPでの取得のほうがいいかも？
    - 1ページでコンテキストを大量に使うので調査目的で取得するのには向かなかった
    - 本当に単体のページを操作する目的で使ったほうがいい
- 日付を範囲指定して取得できるように
    - 必ずしも日時が書いてあるわけではないので件数指定したほうがいい
- 生成した要約を逐次書き出したほうがいい
    - コンテキストのcompactionによる内容の消失を防ぐため
- URLリストの生成の際数が足りない場合はページを遡って取りに行く
    - Fetchで取れないならChrome MCPでもいいかも
        - これはできた
        - ページの遡りができるので、有用
- Listだけ取ったらCompactionしてContext window消費を抑える
    - 特定のコンテキストだけ削除 というのはできなさそう
    - /compact(任意のタイミングでのcompaction呼び出し), /clear(全削除)しかない